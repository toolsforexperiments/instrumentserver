# Monitoring

:::{admonition} 🚧 This page is planned, not yet written
:class: warning
It will be replaced with verified content as the documentation refactor progresses.
:::

This page will cover:

- Concept: Broadcasts → Listener → sink → dashboard
- Polling: making the Server emit without client activity (`pollingRate`)
- The Listener app and listenerConfig; CSV sink; InfluxDB sink
- Writing a custom Listener
- The deployment stack: Docker compose, Grafana + InfluxDB, provisioning, dashboards, alerting
