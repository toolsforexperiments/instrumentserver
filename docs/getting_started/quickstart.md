# Quickstart

:::{admonition} 🚧 This page is planned, not yet written
:class: warning
It will be replaced with verified content as the documentation refactor progresses.
:::

Server + dummy instrument + first client call. This page will cover:

- Starting the server bare (no config file)
- A first client connection; creating a Dummy Instrument via `find_or_create_instrument`
- Getting and setting a parameter; observing the Broadcast
- Starting with a config file (the same setup, declared in YAML)
- Where to go next (links into the User Guide)
